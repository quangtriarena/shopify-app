{
  "name": "arena-new-app",
  "version": "1.0.0",
  "main": "web/index.js",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "scaffold": "shopify app scaffold",
    "deploy": "shopify app deploy",
    "inst": "yarn && cd web/ && yarn && cd frontend/ && yarn",
    "serve": "cd web/ && yarn serve"
  },
  "dependencies": {
    "@shopify/app": "3.6.1",
    "@shopify/cli": "3.6.1"
  },
  "author": "mac"
}
